import {NextResponse} from "next/server";import {publicServerClient} from "@/lib/supabase";import {signedMediaUrl} from "@/lib/minio";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const db=publicServerClient();const {data,error}=await db.from("media_assets").select("provider,object_key,external_url").eq("id",id).single();if(error||!data)return NextResponse.json({error:"Média introuvable"},{status:404});if(data.provider==='external')return NextResponse.redirect(data.external_url);if(!data.object_key)return NextResponse.json({error:"Objet MinIO manquant"},{status:404});return NextResponse.redirect(await signedMediaUrl(data.object_key,900))}
