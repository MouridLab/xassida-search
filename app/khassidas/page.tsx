import { PageHero } from "@/components/site/PageHero";import { CatalogExplorer } from "@/components/khassidas/CatalogExplorer";import { getCatalog } from "@/lib/catalog";
export const metadata={title:"Khassaïdes"};export default async function KhassidasPage(){const {works,stats}=await getCatalog();return <main><PageHero eyebrow="Le corpus" title="Tous les khassaïdes" description="Parcourez les œuvres publiées, consultez leurs sources et reprenez votre lecture dans une interface conçue pour le texte."/><section className="mx-auto max-w-[1400px] px-5 py-12 lg:px-8"><CatalogExplorer works={works} stats={stats}/></section></main>}
