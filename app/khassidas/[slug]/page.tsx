import {notFound} from "next/navigation";import {isConfigured,publicServerClient} from "@/lib/supabase";import {ReaderView} from "@/components/ReaderView";
export default async function WorkPage({params}:{params:Promise<{slug:string}>}){if(!isConfigured)notFound();const {slug}=await params;const db=publicServerClient();const {data:k}=await db.from("khassidas").select("*").eq("slug",slug).eq("is_verified",true).single();if(!k)notFound();const [{data:chunks},{data:related}]=await Promise.all([db.from("khassida_chunks").select("*").eq("khassida_id",k.id).eq("validation_status","verified").order("page_number").limit(150),db.from("khassidas").select("slug,title,arabic_title").eq("is_verified",true).neq("id",k.id).limit(3)]);return <ReaderView work={k} chunks={chunks||[]} related={related||[]}/>}
