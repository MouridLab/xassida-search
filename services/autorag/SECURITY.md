# Security

This service is designed for private network access only. Report vulnerabilities through MouridLab’s private GitHub security advisories, never through a public issue.

Do not include tokens, Supabase credentials, corpus data or production URLs in reports. The service must not ingest untrusted files without a dedicated parser security review.
