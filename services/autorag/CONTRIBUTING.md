# Contributing

Create a focused branch and pull request. Never commit credentials, local corpus files, generated indexes or personal data. Run `npm ci`, compile the Bun entry point and validate the Docker image before submission.

Changes to authentication, data sources, prompts or network exposure must include their security and operational impact.
