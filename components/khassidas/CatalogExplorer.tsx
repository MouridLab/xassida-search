"use client";
import { useMemo, useState } from "react";import { Search, SlidersHorizontal } from "lucide-react";import type { Khassida } from "@/types/database";import type { WorkStats } from "./WorkCard";import { WorkCard } from "./WorkCard";
export function CatalogExplorer({works,stats,initialTheme}:{works:Khassida[];stats:Record<string,WorkStats>;initialTheme?:string}){const [query,setQuery]=useState("");const [theme,setTheme]=useState(initialTheme||"");const themes=useMemo(()=>[...new Set(works.flatMap(w=>w.themes))].filter(Boolean).sort(),[works]);const filtered=useMemo(()=>works.filter(w=>(!theme||w.themes.includes(theme))&&(!query||[w.title,w.arabic_title,...w.aliases,...w.themes].join(" ").toLocaleLowerCase().includes(query.toLocaleLowerCase()))),[works,query,theme]);return <><div className="flex flex-col gap-3 rounded-2xl border border-line bg-surface p-3 shadow-card sm:flex-row"><label className="flex flex-1 items-center gap-3 px-2"><Search size={18} className="text-muted"/><input value={query} onChange={e=>setQuery(e.target.value)} className="h-11 w-full bg-transparent text-sm outline-none" placeholder="Titre, mot arabe ou thème…"/></label><label className="flex items-center gap-2 rounded-xl border border-line px-3 text-sm text-muted"><SlidersHorizontal size={16}/><select value={theme} onChange={e=>setTheme(e.target.value)} className="h-11 min-w-40 bg-transparent outline-none"><option value="">Tous les thèmes</option>{themes.map(item=><option key={item}>{item}</option>)}</select></label></div><p className="mt-6 text-xs font-medium text-muted">{filtered.length} khassida{filtered.length!==1?"s":""}</p><div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{filtered.map(work=><WorkCard key={work.id} work={work} stats={stats[work.id]}/>)}</div>{!filtered.length&&<div className="mt-8 rounded-2xl border border-dashed border-line py-16 text-center text-sm text-muted">Aucun khassida ne correspond à cette recherche.</div>}</>}
